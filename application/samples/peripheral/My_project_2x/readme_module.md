# My_project_2x

## 功能描述
- BS21e 端可运行 demo：广播 12 字节库存字段，接收 WS63 单播写入并执行声光/库存同步。
- 保持三端边界：标签端只执行命令，不做库存历史决策。

## 依赖关系
- 入口在 `app/main.c`，通过 `app_run` 启动。
- 底层由 `components` 目录拆分实现。

## 逻辑验证
- 编译打开 `CONFIG_SAMPLE_SUPPORT_MY_PROJECT_2X=y` 后可生成固件。
- 运行后发送写命令可看到完整状态机日志，且数量广播字段可同步变化。
