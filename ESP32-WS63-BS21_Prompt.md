# 星闪多模态仓储管家系统 —— 三端统一接口与数据流转总纲 (v3.0 终极确立版)

## 1. 角色定义与权力边界 (严禁越权)

为了保证系统高内聚低耦合，三端必须严格恪守以下边界：

* **WS63 (大脑 - 网关中枢)**：
    * **职责**：系统唯一的主控大脑。维护真实的库存数据库，执行核心业务逻辑运算（如：计算出入库后的最终数字、比对盘点差异、分配存放库区），调度星闪网络连接标签。
    * **边界**：向上接收 ESP32 的纯视觉结果和 UI 点击请求；向下通过星闪管理 BS21e 标签。
* **ESP32-S3 (眼与嘴 - 交互端)**：
    * **职责**：负责触摸屏 UI 显示和摄像头视觉识别（纯视觉计数与分类）。
    * **边界**：**绝对无权做业务决策！** 只能上报“我看到了什么（名称/数量）”或“用户点了什么”，绝不能自己做库存加减法。所有的判定结果必须等待 WS63 下发后才能刷新屏幕。
* **BS21e (影子 - 微功耗标签)**：
    * **职责**：物理库位的数字标识。极低功耗广播（宣告存在和展示当前 RAM 里的库存数量），瞬间响应网关的单播连接并执行声光报警。
    * **边界**：**纯粹的执行者！** 不保存历史记录，只被动接收 WS63 发来的新数值或指令。

---

## 2. 三端数据流转全景图 (Data Flow)

所有业务流必须严格遵循下述方向，绝不允许跨级跳跃：

### 业务 1：入库与绑定 (Inbound)
`ESP32(视觉识别:Type-C)` -> `[UART请求]` -> `WS63(运算:分配库区A,建档)` -> `[UART回复]` -> `ESP32(UI提示放入A区)` -> `WS63(发起SLE扫描)` -> `BS21e(被绑定TagID)`

### 业务 2：纯视觉防呆盘点 (Audit)
`WS63(靠近A区,扫描到标签)` -> `[UART指令:启动摄像头盘点]` -> `ESP32(视觉数出视野内数量:48)` -> `[UART汇报:数量48]` -> `WS63(与预期的50做比对,生成异常记录)`

### 业务 3：出库业务 (Outbound)
`ESP32(用户在屏幕确认出库5件)` -> `[UART请求出库]` -> `WS63(本地计算:当前50-5=45件)` -> `[SLE单播Write]` -> `BS21e(接收新值45,刷新广播Payload)`

### 业务 4：声光寻物 (Find)
`ESP32(点击寻找目标)` -> `[UART请求]` -> `WS63(查找对应TagID并拉起SLE直连)` -> `[SLE单播Write: 0x01]` -> `BS21e(点亮声光并开启15s自锁计时)` -> `WS63(迅速断开连接)`

---

## 3. 接口协议 1：ESP32 <== UART ==> WS63 (极速通信链路)

为了提高开发效率和联调速率，放弃复杂的二进制帧头，采用极速纯 JSON 文本协议。

### 3.1 物理层包结构
* **波特率**：`115200 bps` (8N1)。
* **定界符**：每一包 JSON 数据**必须以换行符 `\n` (ASCII: `0x0A`) 作为结束标志**。接收方遇到 `\n` 即可提取字符串进行解析。

### 3.2 核心 JSON 业务字典

| 业务逻辑 | 方向 | 指令 (`cmd`) | 极速载荷范例 (`data`) | 动作说明 |
| :--- | :--- | :--- | :--- | :--- |
| **入库** | ESP->WS63 | `inbound_req` | `{"item": "Type-C"}` | ESP 上报视觉看到的物品 |
| **入库** | WS63->ESP | `inbound_resp`| `{"zone": "A"}` | WS63 算好后，告知 ESP 放去 A 区 |
| **盘点** | WS63->ESP | `start_audit` | `{"tag_id": 12}` | WS63 发现标签，命令 ESP 开启摄像头计数 |
| **盘点** | ESP->WS63 | `audit_report`| `{"qty": 48}` | ESP 拍完照，把数出来的个数传给 WS63 去比对 |
| **出库** | ESP->WS63 | `outbound_req`| `{"item": "Type-C", "qty": 5}`| 用户点击要拿走 5 个，请求 WS63 扣减 |
| **出库** | WS63->ESP | `action_ack` | `{"status": "ok"}` | WS63 扣减成功并刷完标签后，告诉 ESP 刷新 UI |
| **寻物** | ESP->WS63 | `find_tag` | `{"target_id": 12}` | 用户点击寻找某物 |

---

## 4. 接口协议 2：WS63 <== SLE 空口 ==> BS21e (星闪射频链路)

### 4.1 空口广播契约 (12字节 Payload)
WS63 (Scan) 和 BS21e (Adv) 必须包含一套完全一致的 C 语言头文件 `shared_protocol.h`。必须强制 1 字节对齐。

```c
#pragma pack(1) // 确保结构体无多余填充
typedef struct {
    uint32_t magic;      // 固定为 0xAABBCCDD，用于 WS63 极速过滤杂波
    uint16_t tag_id;     // 标签短ID
    uint16_t qty;        // 本地 RAM 镜像的库存数量
    uint8_t status;      // 0x00: 正常, 0x01: 正在寻物报警
    uint8_t battery;     // 电量百分比 (0-100)
    uint16_t seq;        // 序列号（每次数量变更时 seq++）
} shared_proto_adv_field_t;
#pragma pack()
4.2 SSAP 单播极速写入指令
WS63 建立连接后，向 BS21e Write 下列十六进制指令，完成后 立刻执行 Disconnect，释放信道。

0x01：开启声光报警 (标签须自动开启 15 秒硬件定时器防耗电自锁)。

0x00：提前关闭声光。

0x10 [High_Byte] [Low_Byte]：强制覆盖库存数量 (例如 0x10 0x00 0x2D 代表更新数量为 45)。标签收到后立即更新 RAM 变量，并调用底层 API 重置广播 Payload。

5. 三方开发协同 SOP (必读规范)
接口冻结：本文档中的任何 JSON 键值对、字节顺序，未经协商严禁私自更改！

Mock (打桩) 调试极度便利：由于本协议采用了纯 JSON + \n，在 ESP32 还没写好的时候，负责 WS63 的同学可以直接打开电脑的串口助手（如 SSCOM），勾选“加回车换行”，在输入框直接敲击：{"cmd":"find_tag","data":{"target_id":12}}，点击发送即可完美模拟 ESP32 发包，极大提高前后端独立联调效率！

日志红线：所有端侧代码必须在串口打印出完整的状态机切换日志，绝不允许出现“黑盒死机”。